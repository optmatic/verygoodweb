import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    unoptimized: true,
  },
  // siteUrl: 'https://optmatic.com',
  // generateRobotsTxt: true,
  // // build: "next build",
  // // postbuild: "next-sitemap"
};

export default nextConfig;
